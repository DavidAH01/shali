import { HeaderShadowDirectiveDirective } from './header-shadow-directive.directive';

describe('HeaderShadowDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderShadowDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
